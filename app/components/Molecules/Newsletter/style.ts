import { Col, Form } from 'antd';
import styled from 'styled-components';

export const StyleNewsletter = styled(Col)`
  &.o-newsletter{    
  }
`;

export const StyleForm = styled(Form)`
  .o-form{
    box-shadow: 0 0 9px rgba(0, 0, 0, 0.15);
    border-radius: 3px;
    padding: 30px 15px 5px;
  }
`;